'use client';import { motion, useScroll } from 'framer-motion';export default function Home() {  const { scrollYProgress } = useScroll();  const recipes = [    { id: 1, title: 'Italian Pasta', time: '30 mins', image: '/next.svg' },    { id: 2, title: 'French Desserts', time: '45 mins', image: '/vercel.svg' },  ];  return (    <main className="min-h-screen">      <motion.div        className="progress-bar"        style={{ scaleX: scrollYProgress }}      />      {/* Hero Section */}      <section className="px-6 py-20 md:py-32 text-center">        <motion.h1          initial={{ opacity: 0, y: 20 }}          animate={{ opacity: 1, y: 0 }}          transition={{ duration: 0.8 }}          className="text-5xl md:text-7xl font-bold text-orange-600 mb-6"        >          Master the Art of Cooking        </motion.h1>        <motion.p          initial={{ opacity: 0 }}          animate={{ opacity: 1 }}          transition={{ delay: 0.4 }}          className="text-xl text-gray-600 mb-8 max-w-2xl mx-auto"        >          Discover recipes, techniques, and culinary inspiration from around the world.        </motion.p>      </section>      {/* Featured Recipes */}      <section className="px-6 py-16">        <h2 className="text-3xl font-bold text-center mb-12 text-gray-800">          Featured Recipes        </h2>        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">          {recipes.map((recipe, index) => (            <motion.div              key={recipe.id}              initial={{ opacity: 0, scale: 0.9 }}              whileInView={{ opacity: 1, scale: 1 }}              transition={{ delay: index * 0.2 }}              viewport={{ once: true, margin: "-100px" }}              className="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow"            >              <img                src={recipe.image}                alt={recipe.title}                className="w-full h-48 object-cover"              />              <div className="p-6">                <h3 className="text-xl font-semibold mb-2">{recipe.title}</h3>                <p className="text-gray-500">{recipe.time}</p>              </div>            </motion.div>          ))}        </div>      </section>      {/* Why Us Section Link */}      <motion.div        className="text-center py-16"        initial={{ opacity: 0 }}        animate={{ opacity: 1 }}        transition={{ delay: 1.2 }}      >        <a          href="/why-us"          className="inline-block bg-orange-600 text-white px-8 py-3 rounded-lg hover:bg-orange-700 transition-colors text-lg"        >          Why Choose Us?        </a>      </motion.div>    </main>  );}